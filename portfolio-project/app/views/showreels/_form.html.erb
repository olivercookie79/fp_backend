<%= form_with(model: showreel, local: true) do |form| %>
  <% if showreel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(showreel.errors.count, "error") %> prohibited this showreel from being saved:</h2>

      <ul>
      <% showreel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_area :notes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
